<h1>Bookings</h1>
<div class="cards">
  <% @bookings.each do |booking| %>
  <%# <% tent = booking.tent %>
  <div class="card-category" style="background-size: cover; background-position: top; background-image: url(https://images.unsplash.com/photo-1502581424757-92098ba9a0d4?ixlib=rb-1.2.1&ixid=eyJhcHBfaWQiOjEyMDd9&auto=format&fit=crop&w=694&q=80)" >
  <p><%=link_to booking.tent.title, tent_path(booking.tent), class: "btn" %></p>
  <p><%= booking.checkin_date.strftime("%B %d") %> to <%= booking.checkout_date.strftime("%B %d") %></p>
  <%= link_to "Edit", edit_booking_path(booking), class: "btn btn-warning" %>
  <p></p>
  <%= link_to "Remove", booking_path(booking), method: :delete, data: { confirm: "Are you sure?" }, class: "btn btn-warning" %>
  </div>
  <% end %>
</div>

 <%# <% cl_image_tag(booking.tent.photos.first.key) %>
